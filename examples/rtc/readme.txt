First run build_model.py and then simulate_rtc.py

The Python environment must have cht_sfincs

To create the environment, you can do:

conda create -n xmi_test python=3.12
conda activate xmi_test
pip install cht_sfincs@git+https://github.com/deltares-research/cht_sfincs.git
